name: Keep awake
on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping app
        run: curl -fsS -o /dev/null --max-time 60 https://feesaathi.onrender.com/ || true
